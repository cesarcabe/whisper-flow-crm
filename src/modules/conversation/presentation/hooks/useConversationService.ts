/**
 * Re-export from context for convenience
 * This allows importing from hooks directory while maintaining single source of truth
 */
export { useConversation, useConversationService } from '../contexts/ConversationContext';
